function sortByLength (array) {
    return array.sort((a,b) => a.length - b.length)
};
sortByLength(["Beg", "Life", "I", "To"])

//Difficulty 0/5 

//TOP SOLUTION

function sortByLength (array) {
    return array.sort((a,b) => a.length - b.length);
};