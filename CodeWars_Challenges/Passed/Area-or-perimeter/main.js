const areaOrPerimeter = function(l , w) {
    return l === w ? l * w : 2*(l + w);
};

console.log(areaOrPerimeter(5,5))

// Difficulty 1/5 // 


// Top Solutions //

const areaOrPerimetera = function(l , w) {
    return l == w ? l*w : 2*(l + w)
};